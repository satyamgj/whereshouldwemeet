{
  "name": "wheretomeet-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "src/backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install && cd src/backend && npm install --production && cd ../../frontend && npm install",
    "start:backend": "cd src/backend && npm start",
    "start:frontend": "cd frontend && npm start"
  }
}
